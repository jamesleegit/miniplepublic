chrome.runtime.onInstalled.addListener(() => {
    // chrome.tabs.onActivated.addListener((info) => {
    //     // alert(info.tabId);
    //     chrome.tabs.sendMessage(info.tabId, { 'method': 'init' });
    // });
});